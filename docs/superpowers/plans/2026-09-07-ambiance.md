# Ambiance sonore — plan d'implementation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un son court se declenche a intervalle aleatoire pendant qu'OMNI tourne, chez les seuls amis a qui la fonction `ambiance` a ete accordee.

**Architecture:** Une minuterie pure en Node (`src/ambiance.js`) est pilotee depuis `desktop/main.js` par la liste des droits deja tenue a jour par `src/droits/veille.js`. A l'echeance, main.js pousse un evenement IPC a la fenetre, qui joue une balise `<audio>` embarquee dans le paquet. La minuterie vit cote Node parce qu'Electron ralentit les minuteries des fenetres reduites.

**Tech Stack:** Node 20, Electron, `node:test` + `node:assert` (aucune dependance de test).

## Global Constraints

- Conception de reference : `docs/superpowers/specs/2026-09-07-ambiance-design.md`.
- Commentaires et code **sans accents**, comme tout le depot. Les fichiers `.md` peuvent en porter.
- `src/droits/liste.js` et `serveur-maj/lib/fonctions.js` sont deux copies de la meme liste : **toute modification touche les deux**, sinon `test/droits-liste.test.js` echoue.
- Bornes de l'intervalle : **900 000 ms a 1 500 000 ms** (15 a 25 minutes).
- Rafale : **3** coups espaces de **2 500 ms**.
- Volume de lecture : **0.5**.
- Nom de la fonction verrouillable : `ambiance`. Libelle : `ambiance sonore`. Ces deux chaines sont exactes et ne varient pas.
- Chemin du son dans le paquet : `desktop/sons/ping.ogg`. Source : `C:\Users\Utilisateur\Desktop\ping.ogg` (11 881 octets, Ogg Opus).
- Discretion : **aucune entree de devlog** pour cette fonction, et l'avis affiche au changement de droit ne doit jamais nommer `ambiance`.
- Commande de test : `npm test` a la racine (`node --test`). Les 1231 tests existants doivent rester verts.

---

### Task 1: La minuterie

**Files:**
- Create: `src/ambiance.js`
- Test: `test/ambiance.test.js`

**Interfaces:**
- Consumes: rien.
- Produces: `creerAmbiance({ jouer, minMs, maxMs, planifier, arreter, tirage }) -> { demarrer(), stopper(), enMarche() }`
  - `jouer` : `() => void`, appele a chaque echeance.
  - `minMs`, `maxMs` : entiers, bornes incluses du delai tire.
  - `planifier` : signature de `setTimeout`, `(fn, ms) => jeton`. Defaut `setTimeout`.
  - `arreter` : signature de `clearTimeout`, `(jeton) => void`. Defaut `clearTimeout`.
  - `tirage` : `() => number` dans `[0, 1[`. Defaut `Math.random`.
  - `demarrer()` et `stopper()` sont idempotents. `enMarche()` renvoie un booleen.

- [ ] **Step 1: Write the failing test**

Creer `test/ambiance.test.js` :

```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const { creerAmbiance } = require('../src/ambiance');

// Une fausse minuterie: elle retient le rappel et le delai au lieu de dormir.
// Meme procede que test/droits-veille.test.js.
function fauxTemps() {
  let suivant = 1;
  const enAttente = new Map();
  return {
    planifier: (fn, ms) => { const j = suivant++; enAttente.set(j, { fn, ms }); return j; },
    arreter: (j) => { enAttente.delete(j); },
    delais: () => [...enAttente.values()].map((e) => e.ms),
    nombre: () => enAttente.size,
    // Declenche l'unique rappel en attente.
    echoir: () => {
      assert.strictEqual(enAttente.size, 1, 'un seul rappel doit etre en attente');
      const [j, e] = [...enAttente.entries()][0];
      enAttente.delete(j);
      e.fn();
    },
  };
}

test('le delai tombe sur la borne basse quand le tirage vaut 0', () => {
  const t = fauxTemps();
  const a = creerAmbiance({
    jouer: () => {}, minMs: 1000, maxMs: 5000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0,
  });
  a.demarrer();
  assert.deepStrictEqual(t.delais(), [1000]);
});

test('le delai reste dans les bornes quand le tirage frole 1', () => {
  const t = fauxTemps();
  const a = creerAmbiance({
    jouer: () => {}, minMs: 1000, maxMs: 5000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0.999999,
  });
  a.demarrer();
  const [d] = t.delais();
  assert.ok(d >= 1000 && d <= 5000, `delai hors bornes: ${d}`);
});

test('jouer est appele a l echeance, et la minuterie se reprogramme', () => {
  const t = fauxTemps();
  let coups = 0;
  const a = creerAmbiance({
    jouer: () => { coups += 1; }, minMs: 1000, maxMs: 1000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0,
  });
  a.demarrer();
  t.echoir();
  assert.strictEqual(coups, 1);
  assert.strictEqual(t.nombre(), 1, 'elle doit s etre reprogrammee');
  t.echoir();
  assert.strictEqual(coups, 2);
});

test('demarrer deux fois ne lance qu une seule minuterie', () => {
  const t = fauxTemps();
  const a = creerAmbiance({
    jouer: () => {}, minMs: 1000, maxMs: 1000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0,
  });
  a.demarrer();
  a.demarrer();
  assert.strictEqual(t.nombre(), 1);
});

test('stopper annule le rappel en attente et jouer ne part plus', () => {
  const t = fauxTemps();
  let coups = 0;
  const a = creerAmbiance({
    jouer: () => { coups += 1; }, minMs: 1000, maxMs: 1000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0,
  });
  a.demarrer();
  a.stopper();
  assert.strictEqual(t.nombre(), 0);
  assert.strictEqual(coups, 0);
  assert.strictEqual(a.enMarche(), false);
});

test('stopper sans avoir demarre ne leve pas', () => {
  const t = fauxTemps();
  const a = creerAmbiance({
    jouer: () => {}, minMs: 1000, maxMs: 1000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0,
  });
  a.stopper();
  a.stopper();
  assert.strictEqual(a.enMarche(), false);
});

test('une erreur de jouer n arrete pas la minuterie', () => {
  const t = fauxTemps();
  const a = creerAmbiance({
    jouer: () => { throw new Error('la fenetre est partie'); },
    minMs: 1000, maxMs: 1000,
    planifier: t.planifier, arreter: t.arreter, tirage: () => 0,
  });
  a.demarrer();
  t.echoir();
  assert.strictEqual(t.nombre(), 1, 'elle doit s etre reprogrammee malgre l erreur');
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- --test-name-pattern="ambiance"`
Expected: FAIL — `Cannot find module '../src/ambiance'`.

- [ ] **Step 3: Write minimal implementation**

Creer `src/ambiance.js` :

```js
'use strict';

// Une minuterie qui rappelle a intervalle aleatoire, et rien d'autre. Elle ne
// sait pas ce qu'elle declenche, ne connait ni Electron ni les droits: c'est
// desktop/main.js qui l'allume, l'eteint et decide de ce que `jouer` fait.
//
// `planifier`, `arreter` et `tirage` sont injectes pour que les tests soient
// deterministes et instantanes, comme `chercher` l'est dans
// src/droits/veille.js.
function creerAmbiance({
  jouer,
  minMs,
  maxMs,
  planifier = setTimeout,
  arreter = clearTimeout,
  tirage = Math.random,
}) {
  let jeton = null;

  function delai() {
    return minMs + Math.floor(tirage() * (maxMs - minMs + 1));
  }

  function armer() {
    jeton = planifier(() => {
      // Le rappel a echu: le jeton ne designe plus rien. On l'oublie AVANT
      // d'appeler `jouer`, sinon un stopper() declenche depuis `jouer`
      // annulerait un minuteur deja consomme et laisserait `jeton` pose.
      jeton = null;
      // Une fenetre fermee entre deux echeances fait lever l'envoi IPC. Ce
      // n'est pas une raison d'arreter: la minuterie survit a sa fenetre.
      try { jouer(); } catch (e) { /* rien a dire, rien a reparer */ }
      armer();
    }, delai());
  }

  return {
    demarrer() {
      if (jeton !== null) return;   // deja en marche: ne pas doubler
      armer();
    },
    stopper() {
      if (jeton === null) return;
      arreter(jeton);
      jeton = null;
    },
    enMarche() { return jeton !== null; },
  };
}

module.exports = { creerAmbiance };
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test -- --test-name-pattern="ambiance"`
Expected: PASS, 7 tests.

Puis la suite entiere : `npm test`
Expected: tous verts (1231 + 7).

- [ ] **Step 5: Commit**

```bash
git add src/ambiance.js test/ambiance.test.js
git commit -m "feat(ambiance): la minuterie a intervalle aleatoire"
```

---

### Task 2: La fonction verrouillable

**Files:**
- Modify: `src/droits/liste.js`
- Modify: `serveur-maj/lib/fonctions.js`
- Test: `test/droits-liste.test.js` (existe deja, on y ajoute un cas)

**Interfaces:**
- Consumes: rien de la Task 1.
- Produces: le nom `'ambiance'` devient connu de `estConnue()` des deux cotes, et le panneau d'administration affiche automatiquement une case **ambiance sonore** par ami (il lit `FONCTIONS` servie par le serveur).

- [ ] **Step 1: Write the failing test**

Ajouter a la fin de `test/droits-liste.test.js` :

```js
test('ambiance est une fonction connue des deux cotes', () => {
  const app = require('../src/droits/liste');
  const serveur = require('../serveur-maj/lib/fonctions');
  assert.ok(app.estConnue('ambiance'));
  assert.ok(serveur.estConnue('ambiance'));
});
```

Si `test` et `assert` sont deja requis en tete du fichier sous d'autres noms,
reutiliser ces noms plutot que d'en re-`require`.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- --test-name-pattern="ambiance est une fonction connue"`
Expected: FAIL — `estConnue('ambiance')` renvoie `false`.

- [ ] **Step 3: Write minimal implementation**

Dans **les deux** fichiers, ajouter la meme ligne en derniere position du
tableau `FONCTIONS`, juste apres l'entree `no-anim` :

```js
  { nom: 'ambiance', libelle: 'ambiance sonore' },
```

Ne rien changer d'autre : ni l'ordre des entrees existantes, ni les libelles.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test`
Expected: tous verts. `test/droits-liste.test.js` compare les deux listes ;
s'il echoue, c'est qu'un seul des deux fichiers a ete modifie.

- [ ] **Step 5: Commit**

```bash
git add src/droits/liste.js serveur-maj/lib/fonctions.js test/droits-liste.test.js
git commit -m "feat(droits): ambiance sonore, onzieme fonction verrouillable"
```

---

### Task 3: Le son embarque et sa lecture dans la page

**Files:**
- Create: `desktop/sons/ping.ogg` (copie binaire)
- Modify: `desktop/preload.js`
- Modify: `desktop/index.html`
- Test: `test/ambiance-paquet.test.js`

**Interfaces:**
- Consumes: rien de la Task 1 ni de la Task 2.
- Produces: `window.app.surAmbiance(rappel)` cote page, et le canal IPC `'ambiance'` que `desktop/main.js` emettra en Task 4 via `fenetre.webContents.send('ambiance')`.

- [ ] **Step 1: Copier le son**

```bash
mkdir -p desktop/sons
cp "/c/Users/Utilisateur/Desktop/ping.ogg" desktop/sons/ping.ogg
ls -l desktop/sons/ping.ogg
```

Attendu : **11 881 octets**. Si la taille differe, la copie a ete alteree —
recommencer sans passer par un editeur de texte.

- [ ] **Step 2: Write the test**

Creer `test/ambiance-paquet.test.js` :

```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const { ecrireArchive, lireArchive } = require('../amorceur/archive');

const RACINE = path.join(__dirname, '..');
const CHEMIN = 'desktop/sons/ping.ogg';

test('le son existe et c est bien un Ogg', () => {
  const brut = fs.readFileSync(path.join(RACINE, CHEMIN));
  assert.strictEqual(brut.subarray(0, 4).toString('latin1'), 'OggS');
});

// Le paquet part chez les amis en tar.gz maison. Un fichier binaire relu
// octet pour octet, c'est la seule preuve que le son arrivera jouable.
test('le son traverse l archive sans une seule modification', () => {
  const brut = fs.readFileSync(path.join(RACINE, CHEMIN));
  const relu = lireArchive(ecrireArchive([{ chemin: CHEMIN, contenu: brut }]));
  assert.strictEqual(relu.length, 1);
  assert.strictEqual(relu[0].chemin, CHEMIN);
  assert.ok(relu[0].contenu.equals(brut), 'le contenu relu differe de l original');
});

// L'en-tete tar refuse au-dela de 100 caracteres, et faire-paquet-code.js
// leve alors au moment de la fabrication -- trop tard.
test('le chemin du son tient dans un en-tete tar', () => {
  assert.ok(Buffer.byteLength(CHEMIN, 'utf8') <= 100);
});
```

- [ ] **Step 3: Run the test**

Run: `npm test -- --test-name-pattern="son"`

Expected: PASS si l'etape 1 a ete faite ; FAIL avec `ENOENT` sinon. Ces trois
tests ne decrivent pas un comportement a ecrire : ils gardent le fichier contre
une suppression ou une corruption future.

- [ ] **Step 4: Ouvrir le canal dans le preload**

Dans `desktop/preload.js`, a l'interieur de
`contextBridge.exposeInMainWorld('app', { ... })`, juste apres la ligne
`surEtat:` :

```js
  // L'heure du son. Canal en RECEPTION SEULE et sans donnee: il previent, il
  // ne porte rien et n'ouvre ni reseau, ni disque, ni process. La minuterie
  // qui l'emet vit dans desktop/main.js -- Electron ralentit les minuteries
  // des fenetres reduites, et celle d'OMNI passe sa vie derriere le jeu.
  surAmbiance: (rappel) => ipcRenderer.on('ambiance', () => rappel()),
```

`surAmbiance` n'a **pas** d'`ipcMain.handle` correspondant, et c'est normal :
l'avertissement en tete du fichier vise les canaux `invoke`, qui appellent
main.js. Celui-ci va dans l'autre sens, comme `surEtat` et
`surPdaArchiAlerte`.

- [ ] **Step 5: Poser la balise audio et la lecture**

Dans `desktop/index.html`, ajouter la balise juste avant la fermeture
`</body>` :

```html
<audio id="ambiance" src="sons/ping.ogg" preload="auto"></audio>
```

Puis, dans le script de la page, a cote des autres abonnements
(`window.app.surEtat(...)`, `window.app.surPdaArchiAlerte(...)`) :

```js
window.app.surAmbiance(() => {
  const a = document.getElementById('ambiance');
  if (a === null) return;
  a.volume = 0.5;
  // Sans cette remise a zero, un son deja joue jusqu'au bout reste muet: la
  // tete de lecture est restee a la fin.
  a.currentTime = 0;
  // Un refus de lecture automatique ne casse rien et ne se dit pas.
  a.play().catch(() => {});
});
```

- [ ] **Step 6: Run the full suite**

Run: `npm test`
Expected: tous verts.

- [ ] **Step 7: Commit**

```bash
git add desktop/sons/ping.ogg desktop/preload.js desktop/index.html test/ambiance-paquet.test.js
git commit -m "feat(ambiance): le son embarque et sa lecture dans la fenetre"
```

---

### Task 4: Le branchement dans main.js

**Files:**
- Modify: `desktop/main.js` (imports vers la ligne 58 ; creation de la veille et `onChangement` vers les lignes 1267-1310)

**Interfaces:**
- Consumes: `creerAmbiance` (Task 1), le nom de droit `'ambiance'` (Task 2), le canal IPC `'ambiance'` (Task 3).
- Produces: rien pour une tache suivante — c'est la derniere.

- [ ] **Step 1: Importer la minuterie et poser les bornes**

Dans `desktop/main.js`, apres la ligne
`const { creerPorte } = require('../src/droits/porte');` :

```js
const { creerAmbiance } = require('../src/ambiance');
```

Puis, a cote des autres constantes de rythme (vers `PERIODE_PROCESS`,
ligne 61) :

```js
// L'intervalle du son d'ambiance: entre vingt et quatre-vingt-dix minutes.
const AMBIANCE_MIN = 20 * 60 * 1000;
const AMBIANCE_MAX = 90 * 60 * 1000;
```

Et a cote des autres variables de module (vers `let overlay`, ligne 79) :

```js
// La minuterie du son. Creee en meme temps que la veille, allumee et eteinte
// par elle.
let ambiance = null;
```

- [ ] **Step 2: Creer la minuterie et la piloter par les droits**

Juste **avant** l'appel `veille = creerVeille({ ... })` (vers la ligne 1267),
inserer :

```js
  ambiance = creerAmbiance({
    minMs: AMBIANCE_MIN,
    maxMs: AMBIANCE_MAX,
    jouer: () => {
      if (fenetre !== null && !fenetre.isDestroyed()) fenetre.webContents.send('ambiance');
    },
  });
  // OMNI_DEV = le depot lance par outils/lancer-dev.vbs, pas un poste ami. En
  // mode developpement la veille accorde TOUS les droits sans requete: sans
  // cette porte, le son partirait sur la machine de developpement a chaque
  // session. OMNI_AMBIANCE=1 passe outre, pour l'essayer une fois.
  const ambiancePermise = () =>
    veille.droits().includes('ambiance') &&
    (!process.env.OMNI_DEV || process.env.OMNI_AMBIANCE === '1');
  const reglerAmbiance = () => {
    if (ambiancePermise()) ambiance.demarrer();
    else ambiance.stopper();
  };
```

`ambiancePermise` lit `veille`, qui n'est affectee qu'a la ligne suivante :
c'est sans danger, la fonction n'est appelee qu'ensuite. `reglerAmbiance` est
idempotent des deux cotes (Task 1), donc l'appeler a chaque changement ne
double rien.

- [ ] **Step 3: Brancher sur onChangement, et taire l'avis**

Dans le `onChangement` de `creerVeille` (vers la ligne 1284), remplacer les
deux lignes de l'avis :

```js
      if (perdus.length) noterAvis(`Droits : ${perdus.join(', ')} — retiré`);
      else if (gagnes.length) noterAvis(`Droits : ${gagnes.join(', ')} — activé`);
```

par :

```js
      // `ambiance` est la seule exception a la regle du dessus, et elle est
      // deliberee: elle n'a ni interrupteur, ni reglage, rien que l'ami
      // puisse constater comme perdu. L'annoncer ne l'aiderait pas.
      const disparus = perdus.filter((n) => n !== 'ambiance');
      const arrives = gagnes.filter((n) => n !== 'ambiance');
      if (disparus.length) noterAvis(`Droits : ${disparus.join(', ')} — retiré`);
      else if (arrives.length) noterAvis(`Droits : ${arrives.join(', ')} — activé`);
```

Le commentaire existant au-dessus (« UNE FONCTION QUI DISPARAIT EN SILENCE… »)
reste ou il est.

Puis, dans le meme `onChangement`, juste avant `envoyerEtat();` :

```js
      reglerAmbiance();
```

- [ ] **Step 4: Regler l'ambiance au demarrage**

Immediatement apres `veille.demarrer();`, ajouter :

```js
  // Le premier etat vient du cache disque, pose par creerVeille avant meme la
  // premiere requete: sans cet appel, un ami qui a deja le droit ne
  // l'entendrait qu'au premier CHANGEMENT, c'est-a-dire jamais.
  reglerAmbiance();
```

- [ ] **Step 5: Verification manuelle — le son part, meme fenetre reduite**

`desktop/main.js` n'a pas de test automatise ; il se verifie en le lancant.
Depuis la racine, en PowerShell. **Aucune modification de code** : les bornes
se fixent par l'environnement (`bornes()` dans `src/ambiance.js`).

```powershell
$env:OMNI_DEV = "F:\omni_project"
$env:OMNI_AMBIANCE = "1"
$env:OMNI_AMBIANCE_MS = "3000"
npx electron amorceur/electron.js
```

1. Attendu : le son se fait entendre environ toutes les 3 secondes.
2. **Reduire la fenetre** et attendre : le son doit continuer au meme rythme.
   C'est la raison d'etre de la minuterie cote Node ; si le rythme s'effondre,
   la minuterie a fini dans la page par erreur.
3. Fermer.

- [ ] **Step 6: Verification manuelle — le mode developpement est muet**

Dans la meme fenetre PowerShell, retirer le passe-droit et relancer :

```powershell
$env:OMNI_AMBIANCE = $null
npx electron amorceur/electron.js
```

Attendu : **aucun son**, meme apres une minute, alors que l'intervalle est
toujours a 3 secondes.

Fermer ensuite la fenetre PowerShell : les variables ne survivent pas, il n'y
a donc **rien a remettre en place** et aucun `3000` a oublier dans le code.

- [ ] **Step 7: Run the full suite**

Run: `npm test`
Expected: tous verts, aucun test casse par la modification de l'avis.

- [ ] **Step 8: Verifier qu'aucun accent n'a ete introduit dans le code**

```bash
grep -n "[éèêàçùûôîï]" src/ambiance.js test/ambiance.test.js test/ambiance-paquet.test.js
```

Attendu : aucune sortie. Les seuls accents admis dans `desktop/main.js` sont
ceux des chaines affichees qui existaient deja (`— retiré`, `— activé`).

- [ ] **Step 9: Verifier que les vraies bornes sont revenues**

```bash
grep -n "AMBIANCE_MIN\|AMBIANCE_MAX" desktop/main.js
```

Attendu : `20 * 60 * 1000` et `90 * 60 * 1000`. Un `3000` oublie ici et le son
part toutes les trois secondes chez l'ami.

- [ ] **Step 10: Commit**

```bash
git add desktop/main.js
git commit -m "feat(ambiance): la minuterie suit le droit, et se tait en dev"
```

---

## Apres le plan

Ce qui **n'est pas** dans ce plan et se fait a la main, dans l'ordre :

1. **Pas d'entree de devlog.** Le devlog s'affiche dans l'application.
2. Monter la version et fabriquer le paquet par le chemin habituel, puis
   **ouvrir l'archive** et verifier que `desktop/sons/ping.ogg` y est, a
   11 881 octets.
3. Publier, attendre qu'Ilan installe la version.
4. Panneau d'administration : cocher **ambiance sonore** sur sa ligne, et sur
   la sienne seule.
5. Pour arreter : decocher. La veille sonde toutes les 60 s, le son se tait
   dans la minute.
